# 🎬 React Movie App

A movie search and details web app built with **React Router v6.4+** and the **OMDb API**.

## 🚀 Features
- Fetches movie data from OMDb API  
- Uses **loader** and **useLoaderData** for data fetching  
- Implements **dynamic routes** like `/movie/:movieID`  
- Includes a working **contact form** using React Router `<Form>`  
- Uses environment variables for API key  
- Handles errors with a custom `ErrorPage`

## 📦 Installation

```bash
git clone https://github.com/Abdularsf/react-movie-app.git
cd react-movie-app
npm install